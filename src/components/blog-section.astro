---
import { getCollection } from "astro:content";

const posts = (await getCollection("blog")).slice(0, 6)?.map((project) => {
  return project.data;
});
---

{
  posts?.length > 1 && (
    <section class="container mx-auto px-4 py-8 pb-16">
      <div class="max-w-6xl mx-auto">
        <div class="flex items-center justify-between mb-6">
          <h2 class="text-2xl font-bold">Blog Posts</h2>
          <a href="/blog" class="text-sm text-primary hover:underline">
            View all →
          </a>
        </div>
        <div class="grid gap-4 grid-cols-1 md:grid-cols-3">
          {/* {
            blogPosts
              .slice(0, 6)
              .map((post, index) => <ProjectCard key={index} {...post} />)
          } */}
        </div>
      </div>
    </section>
  )
}
